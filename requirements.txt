# Core requirements
Pillow>=10.0.0
geopy>=2.3.0
tqdm>=4.64.0

# Optional for enhanced iPhone support (HEIC/HEIF)
pillow-heif>=0.10.0

# Optional for better video metadata support
hachoir>=3.1.0

# Development dependencies (optional)
pytest>=7.0.0
